{
    "name": "stun-server",
    "$schema": "../../node_modules/nx/schemas/project-schema.json",
    "projectType": "application",
    "sourceRoot": "apps/stun-server/src",
    "implicitDependencies": ["mayhem-db"],
    "targets": {
        "build": {
            "executor": "@monodon/rust:build",
            "outputs": ["{options.target-dir}"],
            "options": {
                "target-dir": "dist/target/stun-server"
            },
            "configurations": {
                "production": {
                    "release": true
                }
            }
        },
        "test": {
            "executor": "@monodon/rust:test",
            "outputs": ["{options.target-dir}"],
            "options": {
                "target-dir": "dist/target/stun-server"
            },
            "configurations": {
                "production": {
                    "release": true
                }
            }
        },
        "lint": {
            "executor": "@monodon/rust:lint",
            "outputs": ["{options.target-dir}"],
            "options": {
                "target-dir": "dist/target/stun-server"
            }
        },
        "run": {
            "executor": "@monodon/rust:run",
            "outputs": ["{options.target-dir}"],
            "options": {
                "target-dir": "dist/target/stun-server"
            },
            "configurations": {
                "production": {
                    "release": true
                }
            }
        }
    },
    "tags": []
}
